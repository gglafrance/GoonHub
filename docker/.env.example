# GoonHub Production Secrets
# Copy to .env and fill in values
#
# Non-secret configuration should be set in config.prod.yaml

# =============================================================================
# Required Secrets
# =============================================================================

# Database password
GOONHUB_DATABASE_PASSWORD=change_me_to_a_strong_password

# PASETO secret for token signing (must be exactly 32 characters)
# Generate with: openssl rand -base64 32 | head -c 32
GOONHUB_AUTH_PASETO_SECRET=change_me_to_32_byte_secret_key!

# Admin account password
GOONHUB_AUTH_ADMIN_PASSWORD=change_me_admin_password

# Meilisearch master key
MEILI_MASTER_KEY=change_me_to_a_strong_meilisearch_key

# =============================================================================
# Optional Settings
# =============================================================================

# GitHub repository for container image (default: goonhub/goonhub)
# GITHUB_REPOSITORY=your-username/goonhub

# Image version/tag (default: latest)
# GOONHUB_VERSION=latest

# External port (default: 8080)
# GOONHUB_PORT=8080

# Path to video files on host (default: ./videos)
# GOONHUB_VIDEOS_PATH=/path/to/your/videos
